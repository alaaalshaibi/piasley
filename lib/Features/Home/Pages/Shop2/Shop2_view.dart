import 'package:flutter/material.dart';
import 'Presentation/Pages/Search/searchBody2.dart';

class Shop2View extends StatelessWidget {
  const Shop2View({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return SearchBody2();
  }
}
